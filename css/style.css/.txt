* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: Arial, sans-serif;
}

body {
  background: #0b0b12;
  color: white;
}

.top {
  text-align: center;
  padding: 20px;
}

.logo {
  width: 200px;
}

.loading {
  position: fixed;
  inset: 0;
  background: black;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
}

.loading-bg {
  position: absolute;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.float {
  width: 200px;
  position: absolute;
  animation: float 3s ease-in-out infinite;
}

.float.a { left: 30%; }
.float.b { right: 30%; animation-delay: 1.5s; }

@keyframes float {
  0% { transform: translateY(0); }
  50% { transform: translateY(-25px); }
  100% { transform: translateY(0); }
}

main {
  padding: 40px;
}

.anniversary {
  margin-bottom: 80px;
}

.banner {
  width: 100%;
  border-radius: 12px;
  margin-bottom: 20px;
}

.monsters {
  display: flex;
  gap: 20px;
  flex-wrap: wrap;
}

.card {
  width: 220px;
  background: #151526;
  border-radius: 16px;
  padding: 12px;
  position: relative;
  text-align: center;
}

.card.mega::before {
  content: "";
  position: absolute;
  inset: -10px;
  background: radial-gradient(circle, rgba(140,0,255,.6), transparent);
  filter: blur(18px);
  z-index: -1;
}

.card img.monster {
  width: 100%;
}

.mega-item {
  width: 40px;
  position: absolute;
  top: 10px;
  right: 10px;
}

.buttons {
  margin-top: 8px;
}

.buttons button {
  margin: 2px;
  padding: 4px 8px;
  background: #2d2d55;
  color: white;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  }
